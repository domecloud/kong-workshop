services:
  code-server:
    image: codercom/code-server:latest
    container_name: code-server
    user: root
    volumes:
      - ./.config:/root/.config
      # - /data:/home/coder/project
    ports:
      - 8080:8080

